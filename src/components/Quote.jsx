import {Col, Container, Row} from "react-bootstrap";

export default function Quote () {
    return(
        <section>
            <Container>
                <Row>
                    <Col>
                    
                    </Col>
                </Row>
            </Container>
        </section>
    );
}